apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-n01683837.fullname" . }}
  labels:
    {{- include "nginx-n01683837.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-n01683837.selectorLabels" . | nindent 4 }}
